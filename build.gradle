// Top-level build file where you can add configuration options common to all sub-projects/modules.

ext {
    githubProjectName = 'rosette'
    versions = [
    ]
    checkstyleFailOnError = false
}

allprojects { project ->
    buildscript {
        repositories {
            mavenCentral()
        }
    }

    repositories {
        mavenCentral()
    }
}

task createWrapper(type: Wrapper) {
    gradleVersion = '1.6'
}